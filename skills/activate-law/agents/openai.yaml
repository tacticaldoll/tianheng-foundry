interface:
  display_name: "Activate Tianheng Law"
  short_description: "Load task-local Tianheng law before coding"
  default_prompt: "Use $activate-law to select the Tianheng boundaries relevant to this Rust change before editing."
policy:
  allow_implicit_invocation: true
