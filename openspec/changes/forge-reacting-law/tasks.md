## 1. Distribution Foundation

- [x] 1.1 Complete collection identity, licensing, and portable host manifests; validate every
  manifest points to the same `skills/` collection.
- [x] 1.2 Add the repository validator and pre-commit/CI entry points for manifests, links,
  compatibility metadata, and OpenSpec artifacts.

## 2. Forge-Law Skill

- [x] 2.1 Initialize `skills/forge-law` with precise Rust/Tianheng activation metadata and
  host-neutral UI metadata.
- [x] 2.2 Implement the eligibility, authority classification, observation admission, generation,
  proof, and prose-disposition workflow in `SKILL.md`.
- [x] 2.3 Add focused references for claim classification, recipe selection, reaction proof, and
  authority transition without copying the full Tianheng cookbook.

## 3. Behavioral Scenarios

- [x] 3.1 Define machine-readable scenarios for eligible existing adoption, explicit first
  adoption, non-Rust refusal, unqualified Rust refusal, mixed-language narrowing, subjective-claim
  decline, and accepted-law amendment refusal.
- [x] 3.2 Implement a network-free scenario validator that proves the corpus covers every verdict
  and safety boundary required by the spec.

## 4. Tianheng Compatibility

- [x] 4.1 Add explicit Tianheng `0.3.x` compatibility metadata and a representative Rust consumer
  fixture covering the recipe vocabulary.
- [x] 4.2 Implement a network-free compatibility runner that accepts `TIANHENG_SOURCE`, patches the
  local Tianheng crate family, and compiles the consumer fixture.

## 5. Verification And Specification

- [x] 5.1 Run skill, plugin, repository, scenario, compatibility, and OpenSpec validation; repair
  every failure without weakening the declared safety boundaries.
- [x] 5.2 Sync the completed capability spec into `openspec/specs/` and leave the change artifacts
  ready for review.
